# Network Tools Menu
# Network diagnostics, monitoring, and connectivity tools

[menu]
name = "Network Tools"
description = "Network diagnostics, monitoring, and connectivity tools"
icon = "🌐"
level = "submenu"

[options]
# Network tools
1 = { display = "Network Diagnostics", action = "script", target = "diagnostics.sh" }
2 = { display = "Bandwidth Monitoring", action = "script", target = "bandwidth-tools.sh" }
3 = { display = "Network Scanners", action = "script", target = "scanners.sh" }
4 = { display = "HTTP/API Tools", action = "script", target = "http-tools.sh" }
5 = { display = "Download Managers", action = "script", target = "download-tools.sh" }
6 = { display = "Remote Access Tools", action = "script", target = "remote-tools.sh" }

# Quick installations
"all" = { display = "Install All Network Tools", action = "install", target = "install.sh" }

# Navigation
"up" = { display = "← Back to Terminal Tools", action = "back", target = ".." }
"main" = { display = "← Main Menu", action = "back", target = "../.." }
"0" = { display = "Exit Archer", action = "exit", target = "" }


[metadata]
requires_network = true
requires_aur = true
estimated_time = "5-15 minutes"
categories = ["network", "monitoring", "diagnostics", "tools"]
